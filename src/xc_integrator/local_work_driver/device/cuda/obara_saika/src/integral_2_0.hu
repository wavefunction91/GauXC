#pragma once

#include "../include/gpu/integral_data_types.hpp"
namespace XGPU {
  void integral_2_0(size_t npts,
		    double *_points,
		    int nprim_pairs,
		    prim_pair *prim_pairs,
		    double *Xi,
		    double *Xj,
		    int ldX,
		    double *Gi,
		    double *Gj,
		    int ldG, 
		    double *weights,
		    double *boys_table);
}
